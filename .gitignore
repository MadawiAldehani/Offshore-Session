# dependencies
/node_modules
/.pnp
.pnp.js

# builds
/.next/
/out/
/build
*.tsbuildinfo
next-env.d.ts

# scratch output from the map cleaner (the cleaned map itself IS committed:
# it is a runtime asset the app serves, not a build artifact)
public/maps/_compare.png

# environment — never commit keys
.env
.env*.local
.env.production

# logs
npm-debug.log*
yarn-error.log*
*.log

# editors / OS
.DS_Store
.vscode/
.idea/
